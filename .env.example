# API Configuration
# URL бэкенд API сервера (с полным путем, включая /api/v1 если нужно)
# В production установите VITE_API_URL в Netlify Dashboard → Site settings → Environment variables
# Например: VITE_API_URL=https://your-backend-url.com/api/v1
VITE_API_URL=http://localhost:8000
