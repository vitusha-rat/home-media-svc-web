# API Configuration
# URL бэкенд API сервера (БЕЗ /api/v1 в конце)
# В production этот URL используется только в Netlify Function для проксирования
# В клиентском коде в production всегда используется относительный путь /api/v1
VITE_API_URL=http://localhost:8000
